Analytics updates:

- Implemented `AnalyticsPage` using `chart.js` and `react-chartjs-2`.
- Displays Strength Score, Body Weight, Calories, and Protein series on a shared timeline with toggles.
- Files changed:
  - strength-tracker-part2/src/modules/analytics/AnalyticsPage.tsx
  - strength-tracker-part2/package.json (added chart dependencies)

Next: Add zoom ranges, date range presets, and tooltips with synchronized crosshairs.
